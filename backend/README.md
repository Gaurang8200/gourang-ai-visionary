# Backend

Reserved for future API services (contact form handler, project data API, analytics).

Planned stack: FastAPI (Python), containerized via `deployment/Dockerfile`.

Nothing deployed from here yet — the site is currently a static frontend served from `frontend/dist`.
